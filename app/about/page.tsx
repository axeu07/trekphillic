import { RouteIntro } from "@/components/ui/RouteIntro";

export const metadata = { title: "About" };

export default function AboutPage() {
  return <RouteIntro title="Travel with intention" description="This space will introduce TrekPhillic, its people, and its approach once the business story is supplied." />;
}