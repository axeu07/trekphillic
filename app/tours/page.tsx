import { RouteIntro } from "@/components/ui/RouteIntro";

export const metadata = { title: "Tours" };

export default function ToursPage() {
  return <RouteIntro title="Go further, together" description="Tour package content will live here once the TrekPhillic team supplies the destinations, itineraries, and imagery." />;
}