import Image from "next/image";
import Link from "next/link";
import type { Trek } from "@/types/trek";

export function FeaturedTrek({ trek }: { trek: Trek }) {
  return <article className="group grid overflow-hidden bg-forest-deep text-white lg:grid-cols-[1.15fr_0.85fr]"><div className="relative min-h-80 overflow-hidden lg:min-h-[32rem]"><Image alt={trek.name} className="object-cover transition duration-700 group-hover:scale-105" fill priority sizes="(min-width: 1024px) 58vw, 100vw" src={trek.coverImage} /><div className="absolute inset-0 bg-gradient-to-t from-forest-deep/65 via-transparent to-transparent" /><p className="absolute bottom-5 left-5 text-xs font-semibold uppercase tracking-[0.18em] text-white/80">Featured trail</p></div><div className="flex flex-col justify-between p-7 sm:p-10 lg:p-12"><div><p className="text-sm text-sage">{trek.location}, {trek.state}</p><h2 className="mt-4 max-w-md font-[family-name:var(--font-poppins)] text-4xl font-semibold leading-[1.05] tracking-[-0.05em] sm:text-5xl">{trek.name}</h2><p className="mt-6 max-w-md text-base leading-7 text-white/72">{trek.shortDescription}</p></div><div className="mt-10"><div className="grid grid-cols-3 gap-4 border-y border-white/20 py-4 text-xs uppercase tracking-[0.12em] text-white/60"><span><strong className="block text-sm tracking-normal text-white">{trek.altitude}</strong>Altitude</span><span><strong className="block text-sm tracking-normal text-white">{trek.duration}</strong>Duration</span><span><strong className="block text-sm capitalize tracking-normal text-white">{trek.difficulty}</strong>Level</span></div><Link className="mt-6 inline-flex items-center gap-3 text-sm font-semibold text-white transition-colors hover:text-sage" href={`/treks/${trek.slug}`}>Explore trail <span aria-hidden="true" className="text-lg">↗</span></Link></div></div></article>;
}