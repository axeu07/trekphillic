import Image from "next/image";
import type { Trek } from "@/types/trek";

export function TrekExperience({ trek }: { trek: Trek }) {
  const [lead, ...supporting] = trek.experience;
  return <section className="mx-auto max-w-7xl px-6 py-20 lg:px-10 lg:py-28"><div className="mb-12 flex flex-col justify-between gap-5 sm:flex-row sm:items-end"><div><p className="text-xs font-semibold uppercase tracking-[0.2em] text-teal">On the trail</p><h2 className="mt-4 font-[family-name:var(--font-poppins)] text-4xl font-semibold tracking-[-0.06em] text-forest-deep sm:text-5xl">What the journey gives back.</h2></div><p className="max-w-xs text-sm leading-6 text-foreground/65">A trek is measured in more than kilometres. These are the moments that shape the days.</p></div><div className="grid gap-6 lg:grid-cols-[1.25fr_0.75fr]">{lead ? <article className="group relative min-h-[28rem] overflow-hidden bg-forest-deep text-white"><Image alt={lead.title} className="object-cover opacity-75 transition duration-700 group-hover:scale-105" fill sizes="(min-width: 1024px) 58vw, 100vw" src={lead.image} /><div className="absolute inset-0 bg-gradient-to-t from-forest-deep/90 via-transparent to-transparent" /><div className="absolute bottom-0 max-w-lg p-7 sm:p-10"><h3 className="text-3xl font-semibold tracking-[-0.05em]">{lead.title}</h3><p className="mt-3 text-sm leading-6 text-white/75">{lead.description}</p></div></article> : null}<div className="grid gap-6">{supporting.map((item) => <article className="border-t border-foreground/20 pt-5" key={item.title}><div className="relative mb-5 aspect-[1.7] overflow-hidden"><Image alt={item.title} className="object-cover transition duration-700 hover:scale-105" fill sizes="(min-width: 1024px) 32vw, 100vw" src={item.image} /></div><h3 className="text-2xl font-semibold tracking-[-0.04em] text-forest-deep">{item.title}</h3><p className="mt-2 max-w-md text-sm leading-6 text-foreground/68">{item.description}</p></article>)}</div></div></section>;
}