import Image from "next/image";
import Link from "next/link";
import type { Trek } from "@/types/trek";

export function TrekHero({ trek }: { trek: Trek }) {
  return <section className="relative isolate min-h-[72svh] overflow-hidden bg-forest-deep text-white"><Image alt={trek.name} className="object-cover" fill priority sizes="100vw" src={trek.coverImage} /><div className="absolute inset-0 bg-gradient-to-r from-forest-deep/90 via-forest-deep/50 to-transparent" /><div className="relative mx-auto flex min-h-[72svh] max-w-7xl items-end px-6 pb-12 pt-32 lg:px-10 lg:pb-16"><div className="max-w-3xl"><p className="text-xs font-semibold uppercase tracking-[0.2em] text-sage">Trek / {trek.location}</p><h1 className="mt-5 max-w-2xl font-[family-name:var(--font-poppins)] text-5xl font-semibold leading-[0.95] tracking-[-0.06em] sm:text-7xl">{trek.name}</h1><p className="mt-6 max-w-lg text-base leading-7 text-white/78">{trek.shortDescription}</p><div className="mt-8 flex flex-wrap items-center gap-x-6 gap-y-3 text-xs uppercase tracking-[0.12em] text-white/70"><span>{trek.altitude}</span><span>{trek.duration}</span><span className="capitalize">{trek.difficulty}</span><span>{trek.bestSeason}</span></div><div className="mt-9 flex flex-wrap gap-5"><Link className="inline-flex min-h-11 items-center justify-center bg-sage px-5 text-sm font-semibold text-forest-deep transition-colors hover:bg-white" href="#plan">Start this journey</Link><Link className="inline-flex min-h-11 items-center border-b border-white/55 text-sm font-semibold text-white transition-colors hover:border-sage hover:text-sage" href="#route">View route</Link></div></div></div></section>;
}