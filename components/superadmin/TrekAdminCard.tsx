import Image from "next/image";
import Link from "next/link";
import type { Trek } from "@/types/trek";

export function TrekAdminCard({ trek }: { trek: Trek }) {
  return <article className="grid gap-5 border-b border-foreground/15 py-5 lg:grid-cols-[10rem_minmax(0,1fr)_auto] lg:items-center"><div className="relative aspect-[1.55] overflow-hidden bg-surface-muted lg:aspect-[1.4] lg:w-40"><Image alt={trek.name} className="object-cover transition duration-500 hover:scale-105" fill sizes="(min-width: 1024px) 10rem, 100vw" src={trek.coverImage} /></div><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><h2 className="text-xl font-semibold tracking-[-0.04em] text-forest-deep">{trek.name}</h2><span className={`px-2 py-1 text-[0.65rem] font-semibold uppercase tracking-[0.12em] ${trek.published ? "bg-sage/45 text-forest-deep" : "bg-sunrise/20 text-[#8a4d1e]"}`}>{trek.published ? "Published" : "Draft"}</span>{trek.featured ? <span className="px-2 py-1 text-[0.65rem] font-semibold uppercase tracking-[0.12em] text-teal">Featured</span> : null}</div><p className="mt-2 text-sm text-foreground/65">{trek.location}, {trek.state}</p><div className="mt-4 flex flex-wrap gap-x-5 gap-y-2 text-xs uppercase tracking-[0.1em] text-foreground/55"><span>{trek.duration}</span><span>{trek.difficulty}</span><span>{trek.altitude}</span><span>Updated from mock data</span></div></div><div className="flex items-center gap-3 lg:justify-end"><Link className="inline-flex min-h-10 items-center justify-center border border-border px-4 text-sm font-semibold text-forest transition-colors hover:border-forest" href={`/superadmin/treks/${trek.id}/edit`}>Edit</Link><details className="relative"><summary className="flex size-10 cursor-pointer list-none items-center justify-center border border-border text-lg text-foreground/70 transition-colors hover:border-forest hover:text-forest" aria-label={`More actions for ${trek.name}`}>...</summary><div className="absolute right-0 top-12 z-10 w-36 border border-border bg-background p-1 shadow-lg"><Link className="block px-3 py-2 text-sm hover:bg-surface-muted" href={`/treks/${trek.slug}`}>Preview</Link><button className="block w-full px-3 py-2 text-left text-sm text-foreground/45" disabled type="button">Duplicate</button><button className="block w-full px-3 py-2 text-left text-sm text-foreground/35" disabled title="Delete will be available when persistence is connected" type="button">Delete</button></div></details></div></article>;
}