import { EditorSection, TextAreaField, TextField } from "@/components/superadmin/trek-editor/EditorSection";
import type { TrekEditorErrors, TrekEditorState } from "@/components/superadmin/trek-editor/types";

export function TrekBasicInfo({ state, errors, onChange }: { state: TrekEditorState; errors: TrekEditorErrors; onChange: <K extends keyof TrekEditorState>(key: K, value: TrekEditorState[K]) => void }) {
  return <EditorSection eyebrow="01 / Identity" title="Basic information"><div className="grid gap-5 sm:grid-cols-2"><TextField error={errors.name} label="Name" onChange={(value) => onChange("name", value)} required value={state.name} /><TextField error={errors.slug} label="Slug" onChange={(value) => onChange("slug", value)} placeholder="chandrashila-trek" required value={state.slug} /><div className="sm:col-span-2"><TextAreaField error={errors.shortDescription} label="Short description" onChange={(value) => onChange("shortDescription", value)} required rows={3} value={state.shortDescription} /></div><TextField label="Location" onChange={(value) => onChange("location", value)} required value={state.location} /><TextField label="State" onChange={(value) => onChange("state", value)} value={state.state} /><TextField label="Starting point" onChange={(value) => onChange("startingPoint", value)} value={state.startingPoint} /><TextField label="Ending point" onChange={(value) => onChange("endingPoint", value)} value={state.endingPoint} /></div></EditorSection>;
}