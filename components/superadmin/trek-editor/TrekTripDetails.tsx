import { EditorSection, TextField } from "@/components/superadmin/trek-editor/EditorSection";
import type { TrekDifficulty } from "@/types/trek";
import type { TrekEditorState } from "@/components/superadmin/trek-editor/types";

export function TrekTripDetails({ state, onChange }: { state: TrekEditorState; onChange: <K extends keyof TrekEditorState>(key: K, value: TrekEditorState[K]) => void }) {
  return <EditorSection eyebrow="02 / The journey" title="Trip details"><div className="grid gap-5 sm:grid-cols-2"><TextField label="Duration" onChange={(value) => onChange("duration", value)} placeholder="3 days" required value={state.duration} /><TextField label="Distance" onChange={(value) => onChange("distance", value)} placeholder="18 km" value={state.distance} /><TextField label="Altitude" onChange={(value) => onChange("altitude", value)} placeholder="12,083 ft" value={state.altitude} /><TextField label="Best season" onChange={(value) => onChange("bestSeason", value)} placeholder="Apr-Jun, Sep-Nov" value={state.bestSeason} /><label className="text-xs font-semibold uppercase tracking-[0.14em] text-foreground/60">Difficulty<span aria-hidden="true" className="ml-1 text-sunrise">*</span><select className="mt-2 min-h-11 w-full border border-border bg-background px-3 text-sm normal-case tracking-normal text-foreground outline-none focus:border-forest" onChange={(event) => onChange("difficulty", event.target.value as TrekDifficulty)} value={state.difficulty}><option value="easy">Easy</option><option value="moderate">Moderate</option><option value="challenging">Difficult</option></select></label></div></EditorSection>;
}